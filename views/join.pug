extends layouts/main.pug

block content
   .form-container
        form(action=routes.postjoin, method="post")
            input(type="text", placeholder="Full Name", name="name", required=true)
            input(type="eamil",placeholder="Email", name="email", required=true)
            input(type="password", placeholder="Password", name="password", required=true)
            input(type="password", placeholder="Password2", name="Verify Password", required=true)
            input(type="submit", value="Join now")
        include partials/socialLogin.pug


